% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media_timestamps.R
\name{update_media_timestamps}
\alias{update_media_timestamps}
\title{Update timestamps for multiple media records}
\usage{
update_media_timestamps(hdr, media_records)
}
\arguments{
\item{hdr}{A base URL provided and valid API key returned by the
function \link{auth_headers}}

\item{media_records}{A data frame or tibble containing the media records to update.
Must contain the following columns:
\itemize{
  \item \strong{media_file_record_id} (required): Numeric. The unique identifier
        of the media file record to update.
  \item \strong{new_timestamp} (required): Character in ISO 8601 format
        (e.g. "2024-01-15T10:30:00", "2024-01-15T10:30:00Z" or
        "2024-01-15T10:30:00+01:00"). A space separator is accepted in
        place of the "T", and POSIXct/Date values are formatted
        automatically.
}
Additional columns will be ignored.}
}
\value{
A list containing the API response with update status
}
\description{
Updates timestamps for one or more media records in a single API call.
For large datasets (>1000 records), use \link{push_new_timestamps} instead.
}
\examples{
\dontrun{
  # Create a data frame with media records to update
  updates <- data.frame(
    media_file_record_id = c(123, 456, 789),
    new_timestamp = c(
      "2024-01-15T10:30:00",
      "2024-01-15T14:20:00",
      "2024-01-15T18:45:00"
    )
  )
  
  # Update the timestamps
  result <- update_media_timestamps(headers, updates)
  
  # Using tibble format
  library(tibble)
  updates <- tibble(
    media_file_record_id = c(123, 456),
    new_timestamp = c("2024-01-15T10:30:00", "2024-01-15T14:20:00")
  )
  result <- update_media_timestamps(headers, updates)
}

}
\seealso{
\link{push_new_timestamps} for automatic chunking of large datasets
}
\author{
Adam Varley
}
